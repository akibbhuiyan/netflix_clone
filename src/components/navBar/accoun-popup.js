"use client";

const AccountPopup = ({
  setPageLoader,
  loggedInAccount,
  setLoggedInAccount,
  setAccount,
  account,
  signOut,
}) => {
  return <div className="">AccountPopup</div>;
};

export default AccountPopup;
